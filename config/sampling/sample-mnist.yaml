dataset:
  dset: MNIST
  is_within_dset: True
  ind: [0, 1, 2, 3, 4, 5, 6, 7]
  ood: [8, 9]

path:
  sample_dir: ../checkpoint/OOD-Sample/

experiment:
  # Use this to specify the sampling regime
  regime: Balanced
  observed_cls: null
